using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testPaula : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int testowanie = 123;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
